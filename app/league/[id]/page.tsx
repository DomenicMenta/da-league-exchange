'use client';
import {FormEvent,useEffect,useMemo,useState} from 'react';
import {useParams} from 'next/navigation';
import AppHeader from '../../components/AppHeader';
import {sampleListings} from '../../marketplace/MarketplaceClient';
type Player={pos:string;name:string;team:string;sleeperId?:string};
type Listing={id:string;title:string;league:string;commissioner:string;format:string;ppr:string;tep:string;type:string;buyIn:number;usual:number;lineup:Player[];verified:boolean;teamCount:number;season:number;description:string;bylawsUrl?:string;bylawsText?:string;draftCapital?:string};
export default function LeagueDetail(){
 const {id}=useParams<{id:string}>(),[listing,setListing]=useState<Listing|null>(null),[missing,setMissing]=useState(false),[applied,setApplied]=useState(false),[saved,setSaved]=useState(false),[sending,setSending]=useState(false);
 useEffect(()=>{fetch(`/api/listings?id=${encodeURIComponent(id)}`).then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>setListing(x.listing)).catch(()=>{const sample=sampleListings.find(x=>x.id===id);if(sample)setListing({...sample,title:'Available franchise',teamCount:12,season:2026,description:'Contact the commissioner through the listing chat or submit an application to learn more about league activity, rules, and long-term expectations.'});else setMissing(true)})},[id]);
 const groups=useMemo(()=>{const result=new Map<string,Player[]>();for(const player of listing?.lineup||[]){const key=player.pos==='SF'?'QB':player.pos;(result.get(key)||result.set(key,[]).get(key)!).push(player)}return [...result]},[listing]);
 async function apply(e:FormEvent<HTMLFormElement>){e.preventDefault();setSending(true);const form=new FormData(e.currentTarget),r=await fetch('/api/applications',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({openingId:id,message:form.get('message'),experience:form.get('experience')})});if(r.status===401){location.href=`/signin?return_to=${encodeURIComponent(location.pathname)}`;return}setApplied(r.ok);setSending(false)}
 if(missing)return <main className="app-page"><AppHeader/><div className="detail-shell"><div className="detail-block"><h1>Listing unavailable</h1><p>This team is no longer open or could not be found.</p><a href="/marketplace">← Return to the Exchange</a></div></div></main>;
 if(!listing)return <main className="app-page"><AppHeader/><div className="detail-shell"><p>Loading full listing…</p></div></main>;
 const discount=listing.buyIn<listing.usual;
 return <main className="app-page"><AppHeader/><div className="detail-shell"><div className="breadcrumbs"><a href="/marketplace">Marketplace</a><span>→</span><span>{listing.league}</span><span>→</span><b>{listing.title}</b></div><section className="detail-hero"><div>{listing.verified&&<span className="verified">✓ VERIFIED SLEEPER LEAGUE</span>}<h1>{listing.title}</h1><p>{listing.league} · {listing.teamCount} Team {listing.format} · {listing.ppr} · {listing.tep}</p><div className="detail-tags"><i>{listing.type}</i><i>ACTIVE LISTING</i><i>{listing.season} SEASON</i></div></div><div className="detail-price"><span>{listing.season} BUY-IN</span><b className={discount?'discount':''}>${listing.buyIn}</b>{discount&&<small>Usually <s>${listing.usual}</s></small>}</div></section><div className="detail-layout"><section className="detail-main"><div className="detail-block"><div className="panel-title"><div><span className="kicker">THE ASSET</span><h2>Starting lineup</h2></div><small>Imported from Sleeper</small></div><div className="position-groups">{groups.map(([pos,players])=><article key={pos}><span>{pos}</span>{players.map((player,i)=><div key={`${player.name}-${i}`}><b>{player.name}</b><em>{player.team||'ROSTERED'}</em></div>)}</article>)}</div>{listing.draftCapital&&<div className="capital"><span><small>DRAFT CAPITAL</small><b>{listing.draftCapital}</b></span></div>}</div><div className="detail-block"><span className="kicker">THE LEAGUE</span><h2>What makes this league special</h2><p>{listing.description||'The commissioner has not added a league description yet.'}</p><div className="fact-grid"><span><small>FORMAT</small><b>{listing.format}</b></span><span><small>SCORING</small><b>{listing.ppr} · {listing.tep}</b></span><span><small>LEAGUE TYPE</small><b>{listing.type}</b></span><span><small>COMMISSIONER</small><b>{listing.commissioner}</b></span></div>{listing.bylawsUrl?<a className="bylaws" href={listing.bylawsUrl} target="_blank" rel="noreferrer">Read complete league bylaws ↗</a>:listing.bylawsText&&<div><h3>League bylaws</h3><p>{listing.bylawsText}</p></div>}</div></section><aside className="apply-card"><div className="commish"><b>{listing.commissioner.slice(0,2).toUpperCase()}</b><span><small>COMMISSIONER</small><strong>{listing.commissioner}</strong><em>Listing owner</em></span></div><p>Apply with your experience and why you are the right manager for this franchise.</p>{applied?<div className="applied"><b>✓</b><h3>Application sent</h3><p>The commissioner has been notified. Track the response from your dashboard.</p><a href="/dashboard?tab=applications">Open dashboard →</a></div>:<form onSubmit={apply}><label>YOUR DYNASTY EXPERIENCE<textarea name="experience" required placeholder="Formats, years playing, current leagues..."/></label><label>WHY THIS TEAM?<textarea name="message" required placeholder="Tell the commissioner why you're a fit..."/></label><button className="primary" disabled={sending}>{sending?'Sending…':'Apply for this team →'}</button></form>}<button className="save" onClick={()=>setSaved(!saved)}>{saved?'★ Saved to watchlist':'☆ Save to watchlist'}</button><small className="apply-note">Applying is free. Your Exchange profile is included.</small></aside></div></div></main>;
}
